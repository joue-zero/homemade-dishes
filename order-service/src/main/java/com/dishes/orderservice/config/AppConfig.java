package com.dishes.orderservice.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AppConfig {
    // RestTemplate bean has been removed to avoid conflict with RestTemplateConfig
} 